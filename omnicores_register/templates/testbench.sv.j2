// ╔═══════════════════════════════════════════════════════════════════════════╗
// ║ Project:     OmniCores-Registers                                          ║
// ║ Author:      Louis Duret-Robert - louisduret@gmail.com                    ║
// ║ Website:     louis-dr.github.io                                           ║
// ║ License:     MIT License                                                  ║{{header_warning_generated_file}}{#
// ╟───────────────────────────────────────────────────────────────────────────╢
// ║ Template:    This is a Jinja2 template using J2GPP and J2GPP extensions.  ║
// ║              It is rendered by the OmniCores-Registers tool.              ║#}
// ╟───────────────────────────────────────────────────────────────────────────╢
// ║ Description: Generated testbench for the register bank to confirm correct ║
// ║              operation.                                                   ║
// ╚═══════════════════════════════════════════════════════════════════════════╝



{% filter restructure %}
module {{register_bank.name}}__register_bank__testbench;
  import {{register_bank.name}}__register_bank__package::*;



  {§ spacing 3 §}



  // Test parameters
  localparam real CLOCK_PERIOD = 10;



  {§ spacing 3 §}



  // Control interface
  logic        control__pclock;
  logic        control__preset_n;
  logic        control__psel;
  logic        control__penable;
  logic        control__pready;
  logic {{register_bank.address_width|arr}} control__paddr;
  logic        control__pwrite;
  logic [31:0] control__pwdata;
  logic [31:0] control__prdata;

  // Control clock generation
  initial begin
    control__pclock = 0;
    forever begin
      #(CLOCK_PERIOD/2) control__pclock = ~control__pclock;
    end
  end

  // Write task
  task automatic register_write;
    input logic {{register_bank.address_width|arr}} address;
    input logic [31:0] data;
    control__psel    = 1;
    control__penable = 0;
    control__pwrite  = 1;
    control__paddr   = address;
    control__pwdata  = data;
    @(negedge control__pclock);
    control__penable = 1;
    while (!control__pready) @(negedge control__pclock);
    control__psel    =  0;
    control__penable = 'x;
    control__pwrite  = 'x;
    control__paddr   = 'x;
    control__pwdata  = 'x;
  endtask

  // Read task
  logic [31:0] expected_data;
  logic [31:0] register_sw_read_data;
  logic [31:0] register_sw_write_data;
  logic [31:0] field_sw_read_data;
  logic [31:0] register_hw_read_data;
  logic [31:0] register_hw_write_data;
  logic [31:0] field_hw_read_data;
  logic [31:0] field_hw_write_data;
  logic [31:0] hw_test_data;
  task automatic register_read;
    input logic {{register_bank.address_width|arr}} address;
    control__psel    = 1;
    control__penable = 0;
    control__pwrite  = 0;
    control__paddr   = address;
    @(negedge control__pclock);
    control__penable = 1;
    while (!control__pready) @(negedge control__pclock);
    register_sw_read_data = control__prdata;
    control__psel    =  0;
    control__penable = 'x;
    control__pwrite  = 'x;
    control__paddr   = 'x;
  endtask



  {§ spacing 3 §}



  // Registers
  {%- filter reindent(1) %}
  {%- filter autoformat_signal_definitions %}
    {%-  for register in register_bank.registers %}
      {%- if register.fields %} {%- for field in register.fields %}
        {%- if field.is_hardware_readable() %}
          {%- if field.has_hw_read_option(HardwareReadOptions.DATA) %}
            logic {{field.width|arr}} register__{{register.hierarchical_name}}__{{field.name}};
          {%- endif %}
          {%- if field.has_hw_read_option(HardwareReadOptions.ANDED) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__anded;
          {%- endif %}
          {%- if field.has_hw_read_option(HardwareReadOptions.ORED) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__ored;
          {%- endif %}
          {%- if field.has_hw_read_option(HardwareReadOptions.XORED) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__xored;
          {%- endif %}
        {%- endif %}
        {%- if field.is_hardware_writable() %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable;
            logic {{field.width|arr}} register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data;
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) %}
            logic {{field.width|arr}} register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data;
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.SET_MASK) %}
            logic {{field.width|arr}} register__{{register.hierarchical_name}}__{{field.name}}__hw_set_mask;
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.SET_ALL) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__hw_set_all;
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.CLEAR_MASK) %}
            logic {{field.width|arr}} register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_mask;
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.CLEAR_ALL) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_all;
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.RESET) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__hw_reset;
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.INCREMENT) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__hw_increment;
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.DECREMENT) %}
            logic                     register__{{register.hierarchical_name}}__{{field.name}}__hw_decrement;
          {%- endif %}
        {%- endif %}
      {%- endfor %} {%- else %}
        {%- if register.is_hardware_readable() %}
          {%- if register.has_hw_read_option(HardwareReadOptions.DATA) %}
            logic {{register.width|arr}} register__{{register.hierarchical_name}};
          {%- endif %}
          {%- if register.has_hw_read_option(HardwareReadOptions.ANDED) %}
            logic                        register__{{register.hierarchical_name}}__anded;
          {%- endif %}
          {%- if register.has_hw_read_option(HardwareReadOptions.ORED) %}
            logic                        register__{{register.hierarchical_name}}__ored;
          {%- endif %}
          {%- if register.has_hw_read_option(HardwareReadOptions.XORED) %}
            logic                        register__{{register.hierarchical_name}}__xored;
          {%- endif %}
        {%- endif %}
        {%- if register.is_hardware_writable() %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
            logic                        register__{{register.hierarchical_name}}__hw_write_enable;
            logic {{register.width|arr}} register__{{register.hierarchical_name}}__hw_write_data;
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) %}
            logic {{register.width|arr}} register__{{register.hierarchical_name}}__hw_write_data;
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.SET_MASK) %}
            logic {{register.width|arr}} register__{{register.hierarchical_name}}__hw_set_mask;
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.SET_ALL) %}
            logic                        register__{{register.hierarchical_name}}__hw_set_all;
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.CLEAR_MASK) %}
            logic {{register.width|arr}} register__{{register.hierarchical_name}}__hw_clear_mask;
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.CLEAR_ALL) %}
            logic                        register__{{register.hierarchical_name}}__hw_clear_all;
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.RESET) %}
            logic                        register__{{register.hierarchical_name}}__hw_reset;
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.INCREMENT) %}
            logic                        register__{{register.hierarchical_name}}__hw_increment;
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.DECREMENT) %}
            logic                        register__{{register.hierarchical_name}}__hw_decrement;
          {%- endif %}
        {%- endif %}
      {%- endif %}
    {%- endfor %}
  {%- endfilter %} {#-autoformat_signal_definitions #}
  {%- endfilter %} {#- reindent #}

  // Register bank under test
  {{register_bank.name}}__register_bank {{register_bank.name}}__register_bank__dut (
    {%- filter remove_blank_lines %}
    {%- filter remove_last_comma %}
    {%- filter autoformat_instance_ports %}
    {%- filter reindent(2) %}

    // Control interface
    .control__pclock   ( control__pclock   ),
    .control__preset_n ( control__preset_n ),
    .control__psel     ( control__psel     ),
    .control__penable  ( control__penable  ),
    .control__pready   ( control__pready   ),
    .control__paddr    ( control__paddr    ),
    .control__pwrite   ( control__pwrite   ),
    .control__pwdata   ( control__pwdata   ),
    .control__prdata   ( control__prdata   ),

    // Registers
    {%- for register in register_bank.registers %}
      {%- if register.fields %} {%- for field in register.fields %}
        {%- if field.is_hardware_readable() %}
          {%- if field.has_hw_read_option(HardwareReadOptions.DATA) %}
            .register__{{register.hierarchical_name}}__{{field.name}} ( register__{{register.hierarchical_name}}__{{field.name}} ),
          {%- endif %}
          {%- if field.has_hw_read_option(HardwareReadOptions.ANDED) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__anded ( register__{{register.hierarchical_name}}__{{field.name}}__anded ),
          {%- endif %}
          {%- if field.has_hw_read_option(HardwareReadOptions.ORED) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__ored ( register__{{register.hierarchical_name}}__{{field.name}}__ored ),
          {%- endif %}
          {%- if field.has_hw_read_option(HardwareReadOptions.XORED) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__xored ( register__{{register.hierarchical_name}}__{{field.name}}__xored ),
          {%- endif %}
        {%- endif %}
        {%- if field.is_hardware_writable() %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable ( register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable ),
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data ( register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data ),
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data ( register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data ),
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.SET_MASK) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_set_mask ( register__{{register.hierarchical_name}}__{{field.name}}__hw_set_mask ),
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.SET_ALL) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_set_all ( register__{{register.hierarchical_name}}__{{field.name}}__hw_set_all ),
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.CLEAR_MASK) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_mask ( register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_mask ),
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.CLEAR_ALL) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_all ( register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_all ),
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.RESET) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_reset ( register__{{register.hierarchical_name}}__{{field.name}}__hw_reset ),
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.INCREMENT) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_increment ( register__{{register.hierarchical_name}}__{{field.name}}__hw_increment ),
          {%- endif %}
          {%- if field.has_hw_write_option(HardwareWriteOptions.DECREMENT) %}
            .register__{{register.hierarchical_name}}__{{field.name}}__hw_decrement ( register__{{register.hierarchical_name}}__{{field.name}}__hw_decrement ),
          {%- endif %}
        {%- endif %}
      {%- endfor %} {%- else %}
        {%- if register.is_hardware_readable() %}
          {%- if register.has_hw_read_option(HardwareReadOptions.DATA) %}
            .register__{{register.hierarchical_name}} ( register__{{register.hierarchical_name}} ),
          {%- endif %}
          {%- if register.has_hw_read_option(HardwareReadOptions.ANDED) %}
            .register__{{register.hierarchical_name}}__anded ( register__{{register.hierarchical_name}}__anded ),
          {%- endif %}
          {%- if register.has_hw_read_option(HardwareReadOptions.ORED) %}
            .register__{{register.hierarchical_name}}__ored ( register__{{register.hierarchical_name}}__ored ),
          {%- endif %}
          {%- if register.has_hw_read_option(HardwareReadOptions.XORED) %}
            .register__{{register.hierarchical_name}}__xored ( register__{{register.hierarchical_name}}__xored ),
          {%- endif %}
        {%- endif %}
        {%- if register.is_hardware_writable() %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
            .register__{{register.hierarchical_name}}__hw_write_enable ( register__{{register.hierarchical_name}}__hw_write_enable ),
            .register__{{register.hierarchical_name}}__hw_write_data ( register__{{register.hierarchical_name}}__hw_write_data ),
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) %}
            .register__{{register.hierarchical_name}}__hw_write_data ( register__{{register.hierarchical_name}}__hw_write_data ),
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.SET_MASK) %}
            .register__{{register.hierarchical_name}}__hw_set_mask ( register__{{register.hierarchical_name}}__hw_set_mask ),
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.SET_ALL) %}
            .register__{{register.hierarchical_name}}__hw_set_all ( register__{{register.hierarchical_name}}__hw_set_all ),
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.CLEAR_MASK) %}
            .register__{{register.hierarchical_name}}__hw_clear_mask ( register__{{register.hierarchical_name}}__hw_clear_mask ),
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.CLEAR_ALL) %}
            .register__{{register.hierarchical_name}}__hw_clear_all ( register__{{register.hierarchical_name}}__hw_clear_all ),
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.RESET) %}
            .register__{{register.hierarchical_name}}__hw_reset ( register__{{register.hierarchical_name}}__hw_reset ),
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.INCREMENT) %}
            .register__{{register.hierarchical_name}}__hw_increment ( register__{{register.hierarchical_name}}__hw_increment ),
          {%- endif %}
          {%- if register.has_hw_write_option(HardwareWriteOptions.DECREMENT) %}
            .register__{{register.hierarchical_name}}__hw_decrement ( register__{{register.hierarchical_name}}__hw_decrement ),
          {%- endif %}
        {%- endif %}
      {%- endif %}
    {%- endfor %}

    {%- endfilter %} {#- reindent #}
    {%- endfilter %} {#- autoformat_instance_ports #}
    {%- endfilter %} {#- remove_last_comma #}
    {%- endfilter %} {#- remove_blank_lines #}
  );



  {§ spacing 3 §}



  // Main block
  initial begin
    // Log waves
    $dumpfile("{{register_bank.name}}__register_bank.testbench.vcd");
    $dumpvars(0, {{register_bank.name}}__register_bank__testbench);
    $timeformat(-9, 0, " ns", 0);


    // Initialization
    control__psel = 0;
    {%- filter align %}
    {%- filter reindent(2) %}
      {%- for register in register_bank.registers %}
        {%- if register.fields %} {%- for field in register.fields %}
          {%- if field.is_hardware_writable() %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable § = 0;
            {%- endif %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data § = 0;
            {%- endif %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.SET_MASK) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_set_mask § = 0;
            {%- endif %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.SET_ALL) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_set_all § = 0;
            {%- endif %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.CLEAR_MASK) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_mask § = 0;
            {%- endif %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.CLEAR_ALL) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_all § = 0;
            {%- endif %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.RESET) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_reset § = 0;
            {%- endif %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.INCREMENT) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_increment § = 0;
            {%- endif %}
            {%- if field.has_hw_write_option(HardwareWriteOptions.DECREMENT) %}
              register__{{register.hierarchical_name}}__{{field.name}}__hw_decrement § = 0;
            {%- endif %}
          {%- endif %}
        {%- endfor %} {%- else %}
          {%- if register.is_hardware_writable() %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
              register__{{register.hierarchical_name}}__hw_write_enable § = 0;
            {%- endif %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) %}
              register__{{register.hierarchical_name}}__hw_write_data § = 0;
            {%- endif %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.SET_MASK) %}
              register__{{register.hierarchical_name}}__hw_set_mask § = 0;
            {%- endif %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.SET_ALL) %}
              register__{{register.hierarchical_name}}__hw_set_all § = 0;
            {%- endif %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.CLEAR_MASK) %}
              register__{{register.hierarchical_name}}__hw_clear_mask § = 0;
            {%- endif %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.CLEAR_ALL) %}
              register__{{register.hierarchical_name}}__hw_clear_all § = 0;
            {%- endif %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.RESET) %}
              register__{{register.hierarchical_name}}__hw_reset § = 0;
            {%- endif %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.INCREMENT) %}
              register__{{register.hierarchical_name}}__hw_increment § = 0;
            {%- endif %}
            {%- if register.has_hw_write_option(HardwareWriteOptions.DECREMENT) %}
              register__{{register.hierarchical_name}}__hw_decrement § = 0;
            {%- endif %}
          {%- endif %}
        {%- endif %}
      {%- endfor %}
    {%- endfilter %} {#- reindent #}
    {%- endfilter %} {#- align #}

    // Reset
    control__preset_n = 0;
    @(negedge control__pclock);
    control__preset_n = 1;


    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 1 : Reset values
    $display("CHECK 1 : Reset values.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.reset_value is not none and not (field.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) and not field.has_hw_write_option(HardwareWriteOptions.ENABLE)) %}
    {§ spacing 1 §}
    expected_data = register__{{register.hierarchical_name}}__{{field.name}}__reset_value;
    {%- if field.is_software_readable() and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect reset value read by software access for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect reset value read by hardware access for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- if field.is_software_readable() and field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(field_hw_read_data === field_sw_read_data)
      else $error("[%t] Different values read by software and software access for register field '{{register.hierarchical_name}}.{{field.name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, field_hw_read_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable and field.is_hardware_readable #}
    {%- endif %} {#- field.reset_value #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.reset_value is not none and not (register.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) and not register.has_hw_write_option(HardwareWriteOptions.ENABLE)) %}
    {§ spacing 1 §}
    expected_data = register__{{register.hierarchical_name}}__reset_value;
    {%- if register.is_software_readable() and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect reset value read by software access for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect reset value read by hardware access for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- if register.is_software_readable() and register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(register_hw_read_data === register_sw_read_data)
      else $error("[%t] Different values read by software and software access for register '{{register.hierarchical_name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, register_hw_read_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable and register.is_hardware_readable #}
    {%- endif %} {#- register.reset_value #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 2 : Software write all ones
    $display("CHECK 2 : Software write all ones.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_software_writable() and field.sw_write_behavior == SoftwareWriteBehavior.NORMAL and not field.is_software_write_once() and not (field.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) and not field.has_hw_write_option(HardwareWriteOptions.ENABLE)) %}
    {§ spacing 1 §}
    expected_data = { {{field.width}} {1'b1}};
    register_sw_write_data = expected_data << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- if field.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read by software access after writing all ones by software for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect value read by hardware access after writing all ones by software for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- if field.is_software_readable() and field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(field_hw_read_data === field_sw_read_data)
      else $error("[%t] Different values read by software and software access for register field '{{register.hierarchical_name}}.{{field.name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, field_hw_read_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable and field.is_hardware_readable #}
    {%- endif %} {#- field.is_software_writable #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_software_writable() and register.sw_write_behavior == SoftwareWriteBehavior.NORMAL and not register.is_software_write_once() and not (register.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) and not register.has_hw_write_option(HardwareWriteOptions.ENABLE)) %}
    {§ spacing 1 §}
    expected_data = { {{register.width}} {1'b1}};
    register_sw_write_data = expected_data;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- if register.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read by software access after writing all ones by software for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect value read by hardware access after writing all ones by software for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- if register.is_software_readable() and register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(register_hw_read_data === register_sw_read_data)
      else $error("[%t] Different values read by software and software access for register '{{register.hierarchical_name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, register_hw_read_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable and register.is_hardware_readable #}
    {%- endif %} {#- register.is_software_writable and not CONTINUOUS-only #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 3 : Software write all zeros
    $display("CHECK 3 : Software write all zeros.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_software_writable() and field.sw_write_behavior == SoftwareWriteBehavior.NORMAL and not field.is_software_write_once() and not (field.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) and not field.has_hw_write_option(HardwareWriteOptions.ENABLE)) %}
    {§ spacing 1 §}
    expected_data = { {{field.width}} {1'b0}};
    register_sw_write_data = expected_data << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- if field.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read by software access after writing all zeros by software for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect value read by hardware access after writing all zeros by software for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- if field.is_software_readable() and field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(field_hw_read_data === field_sw_read_data)
      else $error("[%t] Different values read by software and software access for register field '{{register.hierarchical_name}}.{{field.name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, field_hw_read_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable and field.is_hardware_readable #}
    {%- endif %} {#- field.is_software_writable #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_software_writable() and register.sw_write_behavior == SoftwareWriteBehavior.NORMAL and not register.is_software_write_once() and not (register.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) and not register.has_hw_write_option(HardwareWriteOptions.ENABLE)) %}
    {§ spacing 1 §}
    expected_data = { {{register.width}} {1'b0}};
    register_sw_write_data = expected_data;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- if register.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read by software access after writing all zeros by software for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect value read by hardware access after writing all zeros by software for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- if register.is_software_readable() and register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(register_hw_read_data === register_sw_read_data)
      else $error("[%t] Different values read by software and software access for register '{{register.hierarchical_name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, register_hw_read_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable and register.is_hardware_readable #}
    {%- endif %} {#- register.is_software_writable #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 4 : Hardware write all ones
    $display("CHECK 4 : Hardware write all ones.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
    {§ spacing 1 §}
    expected_data = { {{field.width}} {1'b1}};
    field_hw_write_data = expected_data;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data   = field_hw_write_data;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable =  0;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data   = 'x;
    {%- if field.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read by software access after writing all ones by hardware for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect value read by hardware access after writing all ones by hardware for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- if field.is_software_readable() and field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(field_hw_read_data === field_sw_read_data)
      else $error("[%t] Different values read by software and software access for register field '{{register.hierarchical_name}}.{{field.name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, field_hw_read_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable and field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and ENABLE #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
    {§ spacing 1 §}
    expected_data = { {{register.width}} {1'b1}};
    register_hw_write_data = expected_data;
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = register_hw_write_data;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable =  0;
    register__{{register.hierarchical_name}}__hw_write_data   = 'x;
    {%- if register.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read by software access after writing all ones by hardware for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect value read by hardware access after writing all ones by hardware for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- if register.is_software_readable() and register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(register_hw_read_data === register_sw_read_data)
      else $error("[%t] Different values read by software and software access for register '{{register.hierarchical_name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, register_hw_read_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable and register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and ENABLE #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 5 : Hardware write all zeros
    $display("CHECK 5 : Hardware write all zeros.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
    {§ spacing 1 §}
    expected_data = { {{field.width}} {1'b0}};
    field_hw_write_data = expected_data;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data   = field_hw_write_data;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable =  0;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data   = 'x;
    {%- if field.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read by software access after writing all zeros by hardware for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect value read by hardware access after writing all zeros by hardware for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- if field.is_software_readable() and field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(field_hw_read_data === field_sw_read_data)
      else $error("[%t] Different values read by software and software access for register field '{{register.hierarchical_name}}.{{field.name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, field_hw_read_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable and field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and ENABLE #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
    {§ spacing 1 §}
    expected_data = { {{register.width}} {1'b0}};
    register_hw_write_data = expected_data;
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = register_hw_write_data;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable =  0;
    register__{{register.hierarchical_name}}__hw_write_data   = 'x;
    {%- if register.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read by software access after writing all zeros by hardware for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect value read by hardware access after writing all zeros by hardware for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- if register.is_software_readable() and register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    assert(register_hw_read_data === register_sw_read_data)
      else $error("[%t] Different values read by software and software access for register '{{register.hierarchical_name}}'. Hardware reads '%0h' but software read '%0h'.", $realtime, register_hw_read_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable and register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and ENABLE #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 6 : Continuous write
    $display("CHECK 6 : Continuous write.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) %}
    {§ spacing 1 §}
    expected_data = { {{field.width}} {1'b1}};
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data = expected_data;
    @(negedge control__pclock);
    {%- if field.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read after continuous write for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after continuous write for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data = 'x;
    {%- endif %} {#- field.is_hardware_writable and CONTINUOUS #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.CONTINUOUS) %}
    {§ spacing 1 §}
    expected_data = { {{register.width}} {1'b1}};
    register__{{register.hierarchical_name}}__hw_write_data = expected_data;
    @(negedge control__pclock);
    {%- if register.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value read after continuous write for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after continuous write for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    register__{{register.hierarchical_name}}__hw_write_data = 'x;
    {%- endif %} {#- register.is_hardware_writable and CONTINUOUS #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 7 : Set mask
    $display("CHECK 7 : Set mask.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.SET_MASK) %}
    {§ spacing 1 §}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_set_mask = { {{field.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_set_mask = 0;
    expected_data = { {{field.width}} {1'b1} };
    {%- if field.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after set mask for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after set mask for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and SET_MASK #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.SET_MASK) %}
    {§ spacing 1 §}
    register__{{register.hierarchical_name}}__hw_set_mask = { {{register.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_set_mask = 0;
    expected_data = { {{register.width}} {1'b1} };
    {%- if register.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after set mask for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after set mask for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and SET_MASK #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 8 : Set all
    $display("CHECK 8 : Set all.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.SET_ALL) %}
    {§ spacing 1 §}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_set_all = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_set_all = 0;
    expected_data = { {{field.width}} {1'b1} };
    {%- if field.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after set all for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after set all for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and SET_ALL #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.SET_ALL) %}
    {§ spacing 1 §}
    register__{{register.hierarchical_name}}__hw_set_all = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_set_all = 0;
    expected_data = { {{register.width}} {1'b1} };
    {%- if register.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after set all for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after set all for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and SET_ALL #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 9 : Clear mask
    $display("CHECK 9 : Clear mask.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.CLEAR_MASK) %}
    {§ spacing 1 §}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_mask = { {{field.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_mask = 0;
    expected_data = { {{field.width}} {1'b0} };
    {%- if field.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after clear mask for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after clear mask for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and CLEAR_MASK #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.CLEAR_MASK) %}
    {§ spacing 1 §}
    register__{{register.hierarchical_name}}__hw_clear_mask = { {{register.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_clear_mask = 0;
    expected_data = { {{register.width}} {1'b0} };
    {%- if register.is_software_readable() %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after clear mask for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after clear mask for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and CLEAR_MASK #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 10 : Clear all
    $display("CHECK 10 : Clear all.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.CLEAR_ALL) %}
    {§ spacing 1 §}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_all = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_all = 0;
    expected_data = { {{field.width}} {1'b0} };
    {%- if field.is_software_readable() and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after clear all for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after clear all for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and CLEAR_ALL #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.CLEAR_ALL) %}
    {§ spacing 1 §}
    register__{{register.hierarchical_name}}__hw_clear_all = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_clear_all = 0;
    expected_data = { {{register.width}} {1'b0} };
    {%- if register.is_software_readable() and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after clear all for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after clear all for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and CLEAR_ALL #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 11 : Reset
    $display("CHECK 11 : Reset.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.RESET) and field.reset_value is not none %}
    {§ spacing 1 §}
    expected_data = register__{{register.hierarchical_name}}__{{field.name}}__reset_value;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_reset = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_reset = 0;
    {%- if field.is_software_readable() and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after hw reset for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after hw reset for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and RESET #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.RESET) and register.reset_value is not none %}
    {§ spacing 1 §}
    expected_data = register__{{register.hierarchical_name}}__reset_value;
    register__{{register.hierarchical_name}}__hw_reset = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_reset = 0;
    {%- if register.is_software_readable() and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after hw reset for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after hw reset for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and RESET #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 12 : Increment
    $display("CHECK 12 : Increment.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.INCREMENT) %}
    {§ spacing 1 §}
    expected_data = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_increment = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_increment = 0;
    {%- if field.is_software_readable() and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after increment for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after increment for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and INCREMENT #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.INCREMENT) %}
    {§ spacing 1 §}
    expected_data = 1;
    register__{{register.hierarchical_name}}__hw_increment = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_increment = 0;
    {%- if register.is_software_readable() and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after increment for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after increment for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and INCREMENT #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 13 : Decrement
    $display("CHECK 13 : Decrement.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.DECREMENT) %}
    {§ spacing 1 §}
    expected_data = -1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_decrement = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_decrement = 0;
    {%- if field.is_software_readable() and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read (register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after decrement for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable #}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) and field.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after decrement for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %} {#- field.is_hardware_readable #}
    {%- endif %} {#- field.is_hardware_writable and DECREMENT #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.DECREMENT) %}
    {§ spacing 1 §}
    expected_data = -1;
    register__{{register.hierarchical_name}}__hw_decrement = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_decrement = 0;
    {%- if register.is_software_readable() and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_read (register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after decrement for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) and register.sw_read_behavior == SoftwareReadBehavior.NORMAL %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect hardware value after decrement for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %} {#- register.is_hardware_readable #}
    {%- endif %} {#- register.is_hardware_writable and DECREMENT #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 14 : Hardware read options (AND/OR/XOR reduction)
    $display("CHECK 14 : Hardware read options.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.has_hw_read_option(HardwareReadOptions.DATA) %}
    {%- if field.has_hw_read_option(HardwareReadOptions.ANDED) %}
    {§ spacing 1 §}
    assert(register__{{register.hierarchical_name}}__{{field.name}}__anded === &register__{{register.hierarchical_name}}__{{field.name}})
      else $error("[%t] Incorrect AND reduction for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0b' but got '%0b'.", $realtime, &register__{{register.hierarchical_name}}__{{field.name}}, register__{{register.hierarchical_name}}__{{field.name}}__anded);
    {%- endif %}
    {%- if field.has_hw_read_option(HardwareReadOptions.ORED) %}
    {§ spacing 1 §}
    assert(register__{{register.hierarchical_name}}__{{field.name}}__ored === |register__{{register.hierarchical_name}}__{{field.name}})
      else $error("[%t] Incorrect OR reduction for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0b' but got '%0b'.", $realtime, |register__{{register.hierarchical_name}}__{{field.name}}, register__{{register.hierarchical_name}}__{{field.name}}__ored);
    {%- endif %}
    {%- if field.has_hw_read_option(HardwareReadOptions.XORED) %}
    {§ spacing 1 §}
    assert(register__{{register.hierarchical_name}}__{{field.name}}__xored === ^register__{{register.hierarchical_name}}__{{field.name}})
      else $error("[%t] Incorrect XOR reduction for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0b' but got '%0b'.", $realtime, ^register__{{register.hierarchical_name}}__{{field.name}}, register__{{register.hierarchical_name}}__{{field.name}}__xored);
    {%- endif %}
    {%- endif %} {#- field.has_hw_read_option(DATA) #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.has_hw_read_option(HardwareReadOptions.DATA) %}
    {%- if register.has_hw_read_option(HardwareReadOptions.ANDED) %}
    {§ spacing 1 §}
    assert(register__{{register.hierarchical_name}}__anded === &register__{{register.hierarchical_name}})
      else $error("[%t] Incorrect AND reduction for register '{{register.hierarchical_name}}'. Expected '%0b' but got '%0b'.", $realtime, &register__{{register.hierarchical_name}}, register__{{register.hierarchical_name}}__anded);
    {%- endif %}
    {%- if register.has_hw_read_option(HardwareReadOptions.ORED) %}
    {§ spacing 1 §}
    assert(register__{{register.hierarchical_name}}__ored === |register__{{register.hierarchical_name}})
      else $error("[%t] Incorrect OR reduction for register '{{register.hierarchical_name}}'. Expected '%0b' but got '%0b'.", $realtime, |register__{{register.hierarchical_name}}, register__{{register.hierarchical_name}}__ored);
    {%- endif %}
    {%- if register.has_hw_read_option(HardwareReadOptions.XORED) %}
    {§ spacing 1 §}
    assert(register__{{register.hierarchical_name}}__xored === ^register__{{register.hierarchical_name}})
      else $error("[%t] Incorrect XOR reduction for register '{{register.hierarchical_name}}'. Expected '%0b' but got '%0b'.", $realtime, ^register__{{register.hierarchical_name}}, register__{{register.hierarchical_name}}__xored);
    {%- endif %}
    {%- endif %} {#- register.has_hw_read_option(DATA) #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 15 : Software write behaviors
    $display("CHECK 15 : Software write behaviors.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_software_writable() and field.sw_write_behavior != SoftwareWriteBehavior.NORMAL %}
    {%- if field.is_hardware_writable() and field.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
    {§ spacing 1 §}
    {%- if field.sw_write_behavior == SoftwareWriteBehavior.WRITE_ONE_SETS %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data   = { {{field.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 0;
    expected_data = {{field.width}}'d1;
    register_sw_write_data = expected_data << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-One-Sets for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- elif field.sw_write_behavior == SoftwareWriteBehavior.WRITE_ONE_CLEARS %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data   = { {{field.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 0;
    expected_data = ~{{field.width}}'d1;
    register_sw_write_data = {{field.width}}'d1 << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-One-Clears for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- elif field.sw_write_behavior == SoftwareWriteBehavior.WRITE_ONE_TOGGLES %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data   = { {{field.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 0;
    expected_data = ~{{field.width}}'d1;
    register_sw_write_data = {{field.width}}'d1 << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-One-Toggles for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- elif field.sw_write_behavior == SoftwareWriteBehavior.WRITE_ZERO_SETS %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data = 0;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 0;
    expected_data = ~{{field.width}}'d1;
    register_sw_write_data = {{field.width}}'d1 << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-Zero-Sets for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- elif field.sw_write_behavior == SoftwareWriteBehavior.WRITE_ZERO_CLEARS %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data = ~0;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 0;
    expected_data = {{field.width}}'d1;
    register_sw_write_data = {{field.width}}'d1 << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-Zero-Clears for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- elif field.sw_write_behavior == SoftwareWriteBehavior.WRITE_ZERO_TOGGLES %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data = 0;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 0;
    expected_data = ~{{field.width}}'d1;
    register_sw_write_data = {{field.width}}'d1 << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-Zero-Toggles for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- behavior #}
    {%- endif %} {#- has hw write enable #}
    {%- endif %} {#- is_sw_writable and non-NORMAL #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_software_writable() and register.sw_write_behavior != SoftwareWriteBehavior.NORMAL %}
    {%- if register.is_hardware_writable() and register.has_hw_write_option(HardwareWriteOptions.ENABLE) %}
    {§ spacing 1 §}
    {%- if register.sw_write_behavior == SoftwareWriteBehavior.WRITE_ONE_SETS %}
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = 0;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable = 0;
    expected_data = {{register.width}}'d1;
    register_sw_write_data = expected_data;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-One-Sets for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- elif register.sw_write_behavior == SoftwareWriteBehavior.WRITE_ONE_CLEARS %}
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = { {{register.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable = 0;
    expected_data = ~{{register.width}}'d1;
    register_sw_write_data = {{register.width}}'d1;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-One-Clears for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- elif register.sw_write_behavior == SoftwareWriteBehavior.WRITE_ONE_TOGGLES %}
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = { {{register.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable = 0;
    expected_data = ~{{register.width}}'d1;
    register_sw_write_data = {{register.width}}'d1;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-One-Toggles for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- elif register.sw_write_behavior == SoftwareWriteBehavior.WRITE_ZERO_SETS %}
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = { {{register.width}} {1'b0} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable = 0;
    expected_data = ~{{register.width}}'d1;
    register_sw_write_data = {{register.width}}'d1;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-Zero-Sets for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- elif register.sw_write_behavior == SoftwareWriteBehavior.WRITE_ZERO_CLEARS %}
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = { {{register.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable = 0;
    expected_data = {{register.width}}'d1;
    register_sw_write_data = {{register.width}}'d1;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-Zero-Clears for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- elif register.sw_write_behavior == SoftwareWriteBehavior.WRITE_ZERO_TOGGLES %}
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = { {{register.width}} {1'b0} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable = 0;
    expected_data = ~{{register.width}}'d1;
    register_sw_write_data = {{register.width}}'d1;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after Write-Zero-Toggles for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- behavior #}
    {%- endif %} {#- has hw write enable #}
    {%- endif %} {#- is_sw_writable and non-NORMAL #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 16 : Software read behaviors
    $display("CHECK 16 : Software read behaviors.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_software_readable() and field.sw_read_side_effect_init %}
    {§ spacing 1 §}
    {%- if field.sw_read_behavior == SoftwareReadBehavior.READ_CLEARS -%}
      {%- set before = "{ " ~ field.width ~ " {1'b1} }" -%}
      {%- set after  = "{ " ~ field.width ~ " {1'b0} }" -%}
      {%- set behavior_name = "Read-Clears" -%}
    {%- elif field.sw_read_behavior == SoftwareReadBehavior.READ_SETS -%}
      {%- set before = "{ " ~ field.width ~ " {1'b0} }" -%}
      {%- set after  = "{ " ~ field.width ~ " {1'b1} }" -%}
      {%- set behavior_name = "Read-Sets" -%}
    {%- elif field.sw_read_behavior == SoftwareReadBehavior.READ_RESETS -%}
      {%- set before = field.width ~ "'h" ~ (((2 ** field.width - 1) - field.reset_value)|hexadecimal) -%}
      {%- set after  = field.width ~ "'h" ~ (field.reset_value|hexadecimal) -%}
      {%- set behavior_name = "Read-Resets" -%}
    {%- endif -%}
    {%- if field.sw_read_side_effect_init == 'sw_normal_write' %}
    expected_data = {{before}};
    register_sw_write_data = expected_data << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif field.sw_read_side_effect_init == 'hw_enable_write' %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_data   = {{before}};
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_write_enable = 0;
    {%- elif field.sw_read_side_effect_init == 'sw_write_one_sets' %}
    expected_data = { {{field.width}} {1'b1} };
    register_sw_write_data = expected_data << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif field.sw_read_side_effect_init == 'sw_write_zero_sets' %}
    register_sw_write_data = 0;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif field.sw_read_side_effect_init == 'hw_set_all' %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_set_all = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_set_all = 0;
    {%- elif field.sw_read_side_effect_init == 'hw_set_mask' %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_set_mask = { {{field.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_set_mask = 0;
    {%- elif field.sw_read_side_effect_init == 'sw_write_one_clears' %}
    expected_data = { {{field.width}} {1'b1} };
    register_sw_write_data = expected_data << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif field.sw_read_side_effect_init == 'sw_write_zero_clears' %}
    register_sw_write_data = 0;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif field.sw_read_side_effect_init == 'hw_clear_all' %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_all = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_all = 0;
    {%- elif field.sw_read_side_effect_init == 'hw_clear_mask' %}
    register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_mask = { {{field.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__{{field.name}}__hw_clear_mask = 0;
    {%- endif %}
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    expected_data = {{before}};
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value on first read for register field '{{register.hierarchical_name}}.{{field.name}}' with {{behavior_name}}. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    expected_data = {{after}};
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after {{behavior_name}} for register field '{{register.hierarchical_name}}.{{field.name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- endif %} {#- field.is_software_readable and sw_read_side_effect_init #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_software_readable() and register.sw_read_side_effect_init %}
    {§ spacing 1 §}
    {%- if register.sw_read_behavior == SoftwareReadBehavior.READ_CLEARS -%}
      {%- set before = "{ " ~ register.width ~ " {1'b1} }" -%}
      {%- set after  = "{ " ~ register.width ~ " {1'b0} }" -%}
      {%- set behavior_name = "Read-Clears" -%}
    {%- elif register.sw_read_behavior == SoftwareReadBehavior.READ_SETS -%}
      {%- set before = "{ " ~ register.width ~ " {1'b0} }" -%}
      {%- set after  = "{ " ~ register.width ~ " {1'b1} }" -%}
      {%- set behavior_name = "Read-Sets" -%}
    {%- elif register.sw_read_behavior == SoftwareReadBehavior.READ_RESETS -%}
      {%- set before = register.width ~ "'h" ~ (((2 ** register.width - 1) - register.reset_value)|hexadecimal) -%}
      {%- set after  = register.width ~ "'h" ~ (register.reset_value|hexadecimal) -%}
      {%- set behavior_name = "Read-Resets" -%}
    {%- endif -%}
    {%- if register.sw_read_side_effect_init == 'sw_normal_write' %}
    register_sw_write_data = {{before}};
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif register.sw_read_side_effect_init == 'hw_enable_write' %}
    register__{{register.hierarchical_name}}__hw_write_enable = 1;
    register__{{register.hierarchical_name}}__hw_write_data   = {{before}};
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_write_enable = 0;
    {%- elif register.sw_read_side_effect_init == 'sw_write_one_sets' %}
    register_sw_write_data = { {{register.width}} {1'b1} };
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif register.sw_read_side_effect_init == 'sw_write_zero_sets' %}
    register_sw_write_data = 0;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif register.sw_read_side_effect_init == 'hw_set_all' %}
    register__{{register.hierarchical_name}}__hw_set_all = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_set_all = 0;
    {%- elif register.sw_read_side_effect_init == 'hw_set_mask' %}
    register__{{register.hierarchical_name}}__hw_set_mask = { {{register.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_set_mask = 0;
    {%- elif register.sw_read_side_effect_init == 'sw_write_one_clears' %}
    register_sw_write_data = { {{register.width}} {1'b1} };
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif register.sw_read_side_effect_init == 'sw_write_zero_clears' %}
    register_sw_write_data = 0;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- elif register.sw_read_side_effect_init == 'hw_clear_all' %}
    register__{{register.hierarchical_name}}__hw_clear_all = 1;
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_clear_all = 0;
    {%- elif register.sw_read_side_effect_init == 'hw_clear_mask' %}
    register__{{register.hierarchical_name}}__hw_clear_mask = { {{register.width}} {1'b1} };
    @(negedge control__pclock);
    register__{{register.hierarchical_name}}__hw_clear_mask = 0;
    {%- endif %}
    register_read(register__{{register.hierarchical_name}}__address);
    expected_data = {{before}};
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value on first read for register '{{register.hierarchical_name}}' with {{behavior_name}}. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    register_read(register__{{register.hierarchical_name}}__address);
    expected_data = {{after}};
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after {{behavior_name}} for register '{{register.hierarchical_name}}'. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- endif %} {#- register.is_software_readable and sw_read_side_effect_init #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // Check 17 : Software write-once
    $display("CHECK 17 : Software write-once.");
    {%- for register in register_bank.registers %}
    {%- if register.fields %} {%- for field in register.fields %}
    {%- if field.is_software_write_once() %}
    {§ spacing 1 §}
    expected_data = { {{field.width}} {1'b1} };
    register_sw_write_data = expected_data << register__{{register.hierarchical_name}}__{{field.name}}__offset;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- if field.is_software_readable() %}
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after first write for register field '{{register.hierarchical_name}}.{{field.name}}' with write-once. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- elif field.has_hw_read_option(HardwareReadOptions.DATA) %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect value after first write for register field '{{register.hierarchical_name}}.{{field.name}}' with write-once. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %}
    register_sw_write_data = 0;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- if field.is_software_readable() %}
    register_read(register__{{register.hierarchical_name}}__address);
    field_sw_read_data = (register_sw_read_data & register__{{register.hierarchical_name}}__{{field.name}}__mask) >> register__{{register.hierarchical_name}}__{{field.name}}__offset;
    assert(field_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after second write for register field '{{register.hierarchical_name}}.{{field.name}}' with write-once. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_sw_read_data);
    {%- elif field.has_hw_read_option(HardwareReadOptions.DATA) %}
    field_hw_read_data = register__{{register.hierarchical_name}}__{{field.name}};
    assert(field_hw_read_data === expected_data)
      else $error("[%t] Incorrect value after second write for register field '{{register.hierarchical_name}}.{{field.name}}' with write-once. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, field_hw_read_data);
    {%- endif %}
    {%- endif %} {#- field.is_software_write_once #}
    {%- endfor %} {%- else %} {#- fields #}
    {%- if register.is_software_write_once() %}
    {§ spacing 1 §}
    expected_data = { {{register.width}} {1'b1} };
    register_sw_write_data = expected_data;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- if register.is_software_readable() %}
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after first write for register '{{register.hierarchical_name}}' with write-once. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- elif register.has_hw_read_option(HardwareReadOptions.DATA) %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect value after first write for register '{{register.hierarchical_name}}' with write-once. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %}
    register_sw_write_data = 0;
    register_write(register__{{register.hierarchical_name}}__address, register_sw_write_data);
    {%- if register.is_software_readable() %}
    register_read(register__{{register.hierarchical_name}}__address);
    assert(register_sw_read_data === expected_data)
      else $error("[%t] Incorrect value after second write for register '{{register.hierarchical_name}}' with write-once. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_sw_read_data);
    {%- elif register.has_hw_read_option(HardwareReadOptions.DATA) %}
    register_hw_read_data = register__{{register.hierarchical_name}};
    assert(register_hw_read_data === expected_data)
      else $error("[%t] Incorrect value after second write for register '{{register.hierarchical_name}}' with write-once. Expected '%0h' but got '%0h' instead.", $realtime, expected_data, register_hw_read_data);
    {%- endif %}
    {%- endif %} {#- register.is_software_write_once #}
    {%- endif %} {#- fields #}
    {%- endfor %} {#- registers #}

    repeat(5) @(negedge control__pclock);


    {§ spacing 2 §}


    // End of test
    $finish;
  end



  {§ spacing 3 §}



endmodule
{% endfilter %}
